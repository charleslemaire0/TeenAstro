#pragma once
/**
 * MainUnit.h - Mount + cross-module declarations for TeenAstro Main Unit (C++ build).
 * Replaces the former Global.h; use Command.h / CommandGlobals.h for the command layer.
 */
#include "Mount.h"
#include "MainUnitDecl.h"
