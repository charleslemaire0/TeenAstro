# TeenAstro Documentation (HTML)

Multi-page technical reference. **GitHub does not render HTML** when you open these files in the repo; it shows source.

- **Browse the repo:** use [../README.md](../README.md) as the doc entry point (repo = navigation).
- **Read this HTML site:** clone the repo and open [index.html](index.html) in a browser, or run `python -m http.server 8080` in this folder and open http://localhost:8080.
